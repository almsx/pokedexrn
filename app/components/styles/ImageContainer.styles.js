import {
    StyleSheet
} from 'react-native';
import { Colors } from '../../utils/';

module.exports = StyleSheet.create({
    backImg: {
        top: 0,
        bottom: 0,
        width: '100%',
        height: '100%',
        backgroundColor: Colors.redApp
    }
});